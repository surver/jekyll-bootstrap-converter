RSpec.describe Jekyll::Bootstrap::Converter do
  it "has a version number" do
    expect(Jekyll::Bootstrap::Converter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
